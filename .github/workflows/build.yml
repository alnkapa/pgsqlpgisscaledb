name: Build and Push Docker Image

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions: write-all
    env:
      MY_DOCKER_TOKEN: ${{ secrets.MY_DOCKER_TOKEN }}
    if: ${{ env.MY_DOCKER_TOKEN != '' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: true
      
      - name: Extract Docker image metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ vars.MY_DOCKER_USERNAME }}/pgsqlpgisscaledb
      
      - name: Login to Docker Hub
        if: ${{ vars.MY_DOCKER_REGISTRY != ''}}
        uses: docker/login-action@v4
        with:
          registry: ${{ vars.MY_DOCKER_REGISTRY }}
          username: ${{ vars.MY_DOCKER_USERNAME }}
          password: ${{ env.MY_DOCKER_TOKEN }}
      
      - name: Login to Docker Hub
        if: ${{ vars.MY_DOCKER_REGISTRY == ''}}
        uses: docker/login-action@v4
        with:
          username: ${{ vars.MY_DOCKER_USERNAME }}
          password: ${{ env.MY_DOCKER_TOKEN }}
      
      - name: Set up Docker Buildx
        if: ${{ env.is_LOGIN == 'true' }}
        uses: docker/setup-buildx-action@v4

      - name: Build and push
        if: ${{ env.is_LOGIN == 'true' }}
        uses: docker/build-push-action@v7
        with:
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          annotations: ${{ steps.meta.outputs.annotations }}